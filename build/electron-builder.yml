appId: cn.bit-helper.neuromark
productName: NeuroMark
directories:
  output: release
files:
  - dist/**
  - dist-electron/**
asar: true
asarUnpack:
  - node_modules/sharp/**
  - node_modules/@img/sharp-*/**
  - node_modules/@img/sharp-libvips-*/**
extraResources:
  - from: build/native
    to: native
    filter:
      - '**/*'
  - from: src-native/scancpp/models
    to: models
    filter:
      - '*.onnx'
mac:
  category: public.app-category.education
  artifactName: NeuroMark-${version}-macos-${arch}.${ext}
  target:
    - dmg
win:
  artifactName: NeuroMark-${version}-windows-${arch}-setup.${ext}
  target:
    - nsis
linux:
  artifactName: NeuroMark-${version}-linux-${arch}.${ext}
  target:
    - AppImage
    - deb
  category: Education
